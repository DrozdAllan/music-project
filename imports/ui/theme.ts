import { createTheme } from "@mui/material/styles";

export const myTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#0D6EFD",
    },
    secondary: {
      main: "#CFE2FF",
    },
  },
});
